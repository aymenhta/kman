package com.aymenhta.commands

import com.github.ajalt.clikt.core.CliktCommand

class KmanCLI : CliktCommand(name = "kman", help = "a cli tool to manage your credentials") {
    override fun run() = Unit
}